# LiteLLM proxy (runtime for guardrail)
litellm>=1.69.0

# If using the guardrail as a standalone CLI tool (no extra deps needed)

# Dashboard module imports guardrail packages
# (audit_log and memory_service use stdlib sqlite3 only)
