# LiteLLM proxy (runtime for guardrail)
litellm>=1.69.0

# Optional: structured JSON logging
# GUARDRAIL_LOG_FORMAT=json
python-json-logger>=7.0.0

# Optional: PostgreSQL audit log backend
# GUARDRAIL_AUDIT_BACKEND=postgres
psycopg2-binary>=2.9.0
