/**
 * Legacy Custom404 component — content merged into NotFound.tsx.
 *
 * Kept as a re-export for backward compatibility in case any code
 * still references `Custom404` by name.
 */
export { default } from './NotFound';
